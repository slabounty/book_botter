require "spec_helper"

RSpec.describe BookBotter do
  it "has a version number" do
    expect(BookBotter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
